apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  finalizers:
  - kubernetes.io/pvc-protection
  labels:
    app: saf-boot-starter-sample-allinone-web-prod
    inc-app: saf-boot-starter-sample-allinone-web
  name: saf-boot-starter-sample-allinone-web-prod
  namespace: inc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  volumeName: saf-boot-starter-sample-allinone-web-prod
